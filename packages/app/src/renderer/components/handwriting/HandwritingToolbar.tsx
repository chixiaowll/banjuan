import React from 'react'

export default function HandwritingToolbar() {
  return <div style={{ height: 36, background: 'var(--surface)', borderBottom: '1px solid var(--border)' }}>Toolbar placeholder</div>
}
